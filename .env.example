KEYPAIR=[1,2,3,...,64]

# RPC endpoints
RPC_URL=http://127.0.0.1:8899
WS_URL=ws://127.0.0.1:8900

# Market configuration
MARKET_ID=1

# External price feed
# Option A: set full URL directly (overrides base/token vars)
# PRICE_FEED_URL=http://localhost:8080/api/v1/price/SOL/USDC
PRICE_FEED_BASE_URL=http://localhost:8080/api/v1/price
BASE_TOKEN=SOL
QUOTE_TOKEN=USDC

# Flow divisor (balance / divisor = flow rate)
FLOW_DIVISOR=5
